---
description: serve the pixel arcade locally on port 8080
---

To run the project on `http://localhost:8080/`:

### Option 1: Python (Easiest)
Run the following command in your terminal:
```bash
python -m http.server 8080
```

### Option 2: Node.js / npm
If you have Node.js installed, you can use `serve`:
```bash
npx serve -l 8080 .
```

After running either command, visit [http://localhost:8080/](http://localhost:8080/) in your browser.
